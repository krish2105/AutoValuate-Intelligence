# AutoValuate orchestration API (Render free web service — fits 512 MB, NO torch).
# Comparables embeddings run on fastembed (onnxruntime), not sentence-transformers/torch,
# so the whole API stays well under the free-tier RAM cap (see DECISIONS.md ADR-014).

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20
httpx==0.28.1
sse-starlette==2.1.3

# Agents / orchestration
langgraph==0.2.60
langchain-core==0.3.28

# Valuation model (in-process; SHAP via xgboost native pred_contribs, no shap lib)
xgboost==2.1.3
scikit-learn==1.6.0
joblib==1.4.2
pandas==2.2.3
numpy==2.2.1

# Comparables retrieval — torch-free (fastembed = onnxruntime + tokenizers, ~50 MB)
fastembed==0.5.1
rank-bm25==0.2.2

# LLM providers
google-genai==2.13.0
groq==0.13.0
